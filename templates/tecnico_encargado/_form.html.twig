{% import 'macro/action.html.twig' as action %}

{{ action.frm_start(form) }}
    {{ action.frm_column(
        action.frm_column_field(form.nombreTecnico),
        action.frm_column_field(form.apellidoTecnico)
    ) }}

    {{ action.frm_column(
        action.frm_column_field(form.dni),
        null,
        null,
        null,
        2
    ) }}

    {{ action.frm_column(
        action.frm_column_field(form.direccion),
        null,
        null,
        null,
        2
    ) }}

    {{ action.frm_column(
        action.frm_column_btn(button_label|default('Guardar')),
        null,
        null,
        null,
        2
    ) }}

{{ action.frm_end(form) }}