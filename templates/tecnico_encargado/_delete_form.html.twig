<form method="post" action="{{ path('tecnico_encargado_delete', {'id': tecnico_encargado.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tecnico_encargado.id) }}">
    <button class="btn">Delete</button>
</form>
