<form method="post" action="{{ path('detalle_orden_delete', {'id': detalle_orden.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ detalle_orden.id) }}">
    <button class="btn">Delete</button>
</form>
