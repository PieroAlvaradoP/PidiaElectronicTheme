<form method="post" action="{{ path('orden_servicio_delete', {'id': orden_servicio.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ orden_servicio.id) }}">
    <button class="btn">Delete</button>
</form>
